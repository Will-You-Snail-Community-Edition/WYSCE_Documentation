---
permalink: /functions/modhelper_create_struct
---
# modhelper_create_struct  
&nbsp;  
# Description  
This function was useful back when UndertaleModTool didn't support structs, all it does is return an empty struct. Now that UMT supports structs it is useless unless you're using an older version like the old mod loader "GMSL" does. 
&nbsp;  
# Arguments


